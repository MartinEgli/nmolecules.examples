namespace Samples.Block03.Events
{
    /// <summary>
    /// Kept as a small index file so the block still exposes one obvious entry point,
    /// while the actual invoice-created scenario lives in focused subfolders.
    /// </summary>
    internal static class EventsSample
    {
    }
}
