# Rule Filters

This area contains naming and filter scenarios for Bricks rules.

Use these samples to understand how `Required*`, `Excluded*`, and custom rule
messages narrow dependency diagnostics.
