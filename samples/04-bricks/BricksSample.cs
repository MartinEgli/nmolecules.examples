namespace Samples.Block04.Bricks
{
    /// <summary>
    /// Kept as a small index file so the sample folder still has a single visible
    /// entry point, while the actual examples live in focused files with
    /// scenario-specific names.
    /// </summary>
    internal static class BricksSample
    {
    }
}
